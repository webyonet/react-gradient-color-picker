import { RefObject } from 'react';
declare const usePaintSquare: (canvas: RefObject<HTMLCanvasElement>, hue: number, squareWidth: number, squareHeight: number) => void;
export default usePaintSquare;
